/** STUB — replaced by the graph/backlinks feature agent. Contract: named export, no props. */
export function BacklinksPanel() {
  return <div className="panel-stub">Backlinks (coming soon)</div>;
}
