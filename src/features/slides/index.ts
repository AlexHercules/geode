// Slides presentation mode feature entry (R74). App layer imports the overlay
// from here; splitSlides is exported for the calibration probe parity.
export { SlidesOverlay } from "./SlidesOverlay";
export { splitSlides } from "./slides";
